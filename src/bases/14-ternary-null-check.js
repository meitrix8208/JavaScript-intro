let firstName;
let lastName;

// console.log(`Hola ${firstName ?? "no tiene nombre"} ${lastName || "no tine apellido"}`)

const isActive = true;

const msg = isActive ? "Activo" : "Inactivo";

// if (isActive) {
//   console.log("Activo");
// } else {
//     console.log("Inactivo");
// }

console.log(msg);
